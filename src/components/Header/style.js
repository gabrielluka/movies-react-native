import styled from 'styled-components/native';

const Container = styled.View`
  margin: 20px;
  flex-direction: row;
  justify-content: space-between;
`;

const IconHeader = styled.Image`
  width: 25px;
  height: 25px;
`;

export {IconHeader, Container};
